---
import Base from '../layouts/Base.astro';

const title = 'About Neural Niki';
const description = 'Learn about the author and mission behind this tech blog focused on AI, investing, and software architecture.';

const stats = [
  { value: '15+', label: 'Years Experience' },
  { value: '100+', label: 'Articles Written' },
  { value: '50K+', label: 'Readers Helped' },
  { value: '5', label: 'Topic Areas' },
];

const topics = [
  { icon: '🤖', name: 'AI & ML', description: 'Actual capabilities vs. hype' },
  { icon: '💻', name: 'Architecture', description: 'How systems scale' },
  { icon: '📈', name: 'Investing', description: 'Markets & strategies' },
  { icon: '💰', name: 'Finance', description: 'Building wealth' },
  { icon: '📱', name: 'Digital Culture', description: 'Tech meets society' },
];

const timeline = [
  { year: '2010', title: 'Started in Tech', desc: 'First engineering role at a startup' },
  { year: '2014', title: 'Went Enterprise', desc: 'Joined Fortune 500, learned scale' },
  { year: '2018', title: 'Architecture Lead', desc: 'Designing systems for millions' },
  { year: '2022', title: 'Started Writing', desc: 'Sharing knowledge publicly' },
  { year: '2025', title: 'Neural Niki', desc: 'Launched this publication' },
];
---

<Base {title} {description}>
  <!-- Hero Section -->
  <section class="relative overflow-hidden">
    <div class="absolute inset-0" style="background: linear-gradient(180deg, var(--bg-secondary) 0%, var(--bg-primary) 100%);"></div>
    
    <div class="container-blog relative py-16 md:py-24">
      <div class="max-w-4xl mx-auto">
        <!-- Intro -->
        <div class="text-center mb-16">
          <div 
            class="inline-flex items-center gap-2 px-4 py-2 rounded-full text-sm font-medium mb-6"
            style="background-color: var(--accent-light); color: var(--accent-primary);"
          >
            👋 Nice to meet you
          </div>
          
          <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold mb-6" style="color: var(--text-primary);">
            I write about <span class="gradient-text">technology</span> so you don't have to read the boring parts.
          </h1>
          
          <p class="text-lg md:text-xl max-w-2xl mx-auto" style="color: var(--text-tertiary);">
            Deep dives into AI hype, software architecture secrets, investing psychology, and the hidden mechanics of the digital world.
          </p>
        </div>
        
        <!-- Stats Grid -->
        <div 
          class="grid grid-cols-2 md:grid-cols-4 gap-4 p-6 rounded-2xl"
          style="background-color: var(--bg-elevated); border: 1px solid var(--border-primary);"
        >
          {stats.map(stat => (
            <div class="text-center py-4">
              <div class="text-3xl md:text-4xl font-bold mb-1 gradient-text">{stat.value}</div>
              <div class="text-sm font-medium" style="color: var(--text-tertiary);">{stat.label}</div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Mission Section -->
  <section class="container-blog py-16 md:py-24">
    <div class="max-w-4xl mx-auto">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        <!-- Text -->
        <div>
          <h2 class="text-2xl md:text-3xl font-bold mb-6" style="color: var(--text-primary);">
            The Mission
          </h2>
          <div class="space-y-4" style="color: var(--text-secondary);">
            <p>
              In an age of clickbait and shallow takes, <strong style="color: var(--text-primary);">Neural Niki exists to provide something different</strong>: thoughtful, long-form content that respects your intelligence.
            </p>
            <p>
              I write about real-world technology stories—from the ambitious failures of AI startups to the hidden architecture that powers the apps you use daily.
            </p>
            <p>
              Every article aims to leave you with a deeper understanding, actionable insights, and questions that make you think differently.
            </p>
          </div>
        </div>
        
        <!-- Visual Card -->
        <div 
          class="relative p-8 rounded-2xl overflow-hidden"
          style="background: linear-gradient(135deg, #7c3aed, #8b5cf6);"
        >
          <div class="absolute top-0 right-0 w-32 h-32 rounded-full opacity-30 blur-2xl" style="background: #c4b5fd;"></div>
          
          <div class="relative">
            <blockquote class="text-xl md:text-2xl font-medium text-white leading-relaxed">
              "The best way to understand technology is to understand the people building it—their incentives, constraints, and blind spots."
            </blockquote>
            <div class="mt-6 flex items-center gap-3">
              <div class="w-10 h-10 rounded-full flex items-center justify-center text-lg" style="background: rgba(255,255,255,0.2);">
                ✍️
              </div>
              <div>
                <div class="font-semibold text-white">Neural Niki</div>
                <div class="text-sm text-white/70">Author</div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Topics Section -->
  <section class="py-16 md:py-24" style="background-color: var(--bg-secondary);">
    <div class="container-blog">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-2xl md:text-3xl font-bold mb-4 text-center" style="color: var(--text-primary);">
          What I Write About
        </h2>
        <p class="text-center mb-12 max-w-xl mx-auto" style="color: var(--text-tertiary);">
          Five topic areas, one mission: making complex things understandable.
        </p>
        
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-4">
          {topics.map(topic => (
            <div 
              class="group p-5 rounded-xl text-center transition-all hover:-translate-y-1 hover:shadow-lg"
              style="background-color: var(--bg-elevated); border: 1px solid var(--border-primary);"
            >
              <div class="text-3xl mb-3 transition-transform group-hover:scale-110">{topic.icon}</div>
              <h3 class="font-semibold mb-1" style="color: var(--text-primary);">{topic.name}</h3>
              <p class="text-xs" style="color: var(--text-muted);">{topic.description}</p>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Timeline Section -->
  <section class="container-blog py-16 md:py-24">
    <div class="max-w-2xl mx-auto">
      <h2 class="text-2xl md:text-3xl font-bold mb-12 text-center" style="color: var(--text-primary);">
        The Journey
      </h2>
      
      <div class="relative">
        <!-- Timeline line -->
        <div 
          class="absolute left-4 md:left-1/2 top-0 bottom-0 w-px -translate-x-1/2"
          style="background: linear-gradient(180deg, var(--accent-primary), var(--border-primary));"
        ></div>
        
        <!-- Timeline items -->
        <div class="space-y-8">
          {timeline.map((item, index) => (
            <div class:list={["relative flex items-start gap-6", index % 2 === 0 ? "md:flex-row" : "md:flex-row-reverse"]}>
              <!-- Dot -->
              <div 
                class="absolute left-4 md:left-1/2 w-3 h-3 rounded-full -translate-x-1/2 border-2 z-10"
                style="background-color: var(--bg-primary); border-color: var(--accent-primary);"
              ></div>
              
              <!-- Content -->
              <div class:list={["ml-10 md:ml-0 md:w-1/2", index % 2 === 0 ? "md:pr-12 md:text-right" : "md:pl-12"]}>
                <div 
                  class="inline-block px-3 py-1 rounded-full text-xs font-bold mb-2"
                  style="background-color: var(--accent-light); color: var(--accent-primary);"
                >
                  {item.year}
                </div>
                <h3 class="font-semibold mb-1" style="color: var(--text-primary);">{item.title}</h3>
                <p class="text-sm" style="color: var(--text-tertiary);">{item.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- Editorial Standards -->
  <section class="py-16 md:py-24" style="background-color: var(--bg-secondary);">
    <div class="container-blog">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-2xl md:text-3xl font-bold mb-12 text-center" style="color: var(--text-primary);">
          Editorial Standards
        </h2>
        
        <div class="grid md:grid-cols-2 gap-6">
          {[
            { icon: '✓', title: 'Accuracy First', desc: 'All claims are fact-checked and sourced where possible' },
            { icon: '🚫', title: 'No Sponsored Content', desc: 'I never accept payment for articles or reviews' },
            { icon: '🔍', title: 'Transparent Disclosure', desc: 'Any potential conflicts of interest are clearly disclosed' },
            { icon: '✏️', title: 'Corrections Welcome', desc: 'I update articles when errors are pointed out' },
          ].map(item => (
            <div 
              class="flex gap-4 p-5 rounded-xl"
              style="background-color: var(--bg-elevated); border: 1px solid var(--border-primary);"
            >
              <div 
                class="flex-shrink-0 w-10 h-10 rounded-lg flex items-center justify-center text-lg"
                style="background-color: var(--accent-light);"
              >
                {item.icon}
              </div>
              <div>
                <h3 class="font-semibold mb-1" style="color: var(--text-primary);">{item.title}</h3>
                <p class="text-sm" style="color: var(--text-tertiary);">{item.desc}</p>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="container-blog py-16 md:py-24">
    <div class="max-w-2xl mx-auto text-center">
      <h2 class="text-2xl md:text-3xl font-bold mb-4" style="color: var(--text-primary);">
        Let's Connect
      </h2>
      <p class="mb-8" style="color: var(--text-tertiary);">
        Have a story idea, correction, or just want to chat? I'd love to hear from you.
      </p>
      
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a href="/contact" class="btn-primary px-8 py-3">
          Get in Touch
        </a>
        <a 
          href="https://twitter.com/neuralniki"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-secondary px-8 py-3"
        >
          Follow on X
        </a>
      </div>
      
      <p class="mt-8 text-sm" style="color: var(--text-muted);">
        All opinions expressed here are my own and do not represent any employer or organization.
      </p>
    </div>
  </section>
</Base>
